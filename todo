Te has quedado haciendo los usuarios:
Done in last sesion:
        o Crear los tags para restaurantes en la base de datos
ToDo in next session:
    - Solucionar bugs: Básicamente has creado review_tag con tag_id, review_id. Pero la review en verdad era user_id y restaurant_id. Así que ahora hay un mix caótico donde hay llamadas del backend (todo es en el backend) que usan un review_id y otras que usan un user_id & restaurant_id.
        - Cambiado en restaurant_review de user_id&restaurant_id a review_id primary key. 
        - En un principio todo funcionaría bien ahora. Las llamadas que usen restaurant_id&user_id, siguen usandolo (si se crea una review como es ai no pasa nada, para eliminar se busca por user_id y restaurant_id, para update lo mismo...)

ToDo in future
    - Bug al añadir un restaurante al top 5 directamente, no se quita el anterior así que hay dos en la base de datos con ranking X
    - Explorar para meter apis de google maps
        